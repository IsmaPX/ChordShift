-- Migration: add audio storage, ear-training endpoints, settings, shares, leaderboard
-- Generated manually based on schema.prisma

-- Este archivo es un marcador. La migración real se genera con:
-- cd apps/api && pnpm prisma:migrate --name add_fase3_features

-- Una vez generada, Prisma creará automáticamente las tablas y columnas.
-- Los endpoints nuevos (audio, ear-training, settings, shares, leaderboard)
-- funcionan contra el schema actual sin cambios adicionales.

-- Recordatorio: ejecutar `pnpm prisma:generate` tras cambios en schema.prisma
