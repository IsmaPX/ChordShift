{
  "framework": "vite",
  "buildCommand": "echo 'build done locally'",
  "devCommand": "pnpm --filter=web dev",
  "installCommand": "echo 'deps installed locally'",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}